export default class {
    constructor(id = undefined, actorId, firstName, lastName, review, rating, author) {
      this.id = id;
      this.actorId = actorId;
      this.firstName = firstName;
      this.lastName = lastName;
      this.review = review;
      this.rating = rating;
      this.author = author;
    }
  }
