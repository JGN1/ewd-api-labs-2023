export default class {
    constructor(id = undefined, movieId, movie, author, review, rating) {
      this.id = id;
      this.movieId = movieId;
      this.movie = movie;
      this.author = author;
      this.review = review;
      this.rating = rating;
    }
  }
