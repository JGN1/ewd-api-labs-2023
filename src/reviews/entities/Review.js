export default class {
    constructor(id = undefined, movieId, movie, author, content, rating) {
      this.id = id;
      this.movieId = movieId;
      this.movie = movie;
      this.author = author;
      this.content = content;
      this.rating = rating;
    }
  }
